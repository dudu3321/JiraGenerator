import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './SqlData.styles';

const SqlData = (props) => (
  <div className="SqlDataWrapper">
   SqlDataWrapper
  </div>
);

SqlData.propTypes = {
  // bla: PropTypes.string,
};

SqlData.defaultProps = {
  // bla: 'test',
};

export default SqlData;
